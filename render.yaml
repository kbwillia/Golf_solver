services:
  - type: web
    name: golf-card-game
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 1 wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7