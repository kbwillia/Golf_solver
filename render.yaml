services:
  - type: web
    name: golf-card-game
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python run_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16